{
  "_meta": {
    "framework": "mitre_attack",
    "framework_full_name": "MITRE ATT&CK Enterprise (Cloud IaaS sub-matrix)",
    "description": "Mapping of finding types to MITRE ATT&CK techniques an attacker could use to exploit them. This is a threat framework, not a compliance framework — references show which attacker techniques the misconfiguration enables. Techniques are drawn from the Cloud IaaS platform sub-matrix.",
    "source_url": "https://attack.mitre.org/matrices/enterprise/cloud/iaas/",
    "mapping_rationale": "MITRE ATT&CK catalogues attacker behaviour. Not every misconfiguration cleanly maps to a specific technique — some are defensive control weaknesses that don't directly enable an attacker action, and forcing a mapping in those cases produces misleading noise. Mappings below prioritise honesty over completeness.",
    "audit_notes": {
      "S3_PUBLIC_VIA_ACL": "T1530 (Data from Cloud Storage) is a direct match. An attacker downloading objects from a publicly-readable bucket is exactly what this technique describes.",
      "S3_PUBLIC_ACCESS_BLOCK_DISABLED": "Corrected from an earlier draft that mapped this to T1078.004 (Valid Accounts: Cloud Accounts). That mapping was wrong — T1078.004 is about compromised credentials, and PAB being off doesn't enable credential-based attacks. T1580 (Cloud Infrastructure Discovery) is a weaker but more honest fit: an attacker in reconnaissance can discover misconfigured buckets more easily when PAB is off. Mapping defensive control weaknesses to attacker techniques is inherently forced; documented here rather than papered over.",
      "S3_ENCRYPTION_DISABLED": "T1530 covers this by extension — data extracted from cloud storage is more valuable and usable if not encrypted at rest. This is a secondary consequence rather than a primary enablement, but it is defensible.",
      "S3_VERSIONING_DISABLED": "No technique directly matches — disabling versioning is not an attacker action, it's the absence of a recovery mechanism. Mapped to T1485 (Data Destruction) as a weaker, honest fit, following the same reasoning as the PAB mapping above: this misconfiguration doesn't enable a destructive attack, but it removes the safety net that would otherwise let the organisation undo one.",
      "S3_LOGGING_DISABLED": "T1562.008 (Disable or Modify Cloud Logs) is an imperfect fit and documented as such: the technique describes an attacker actively turning logging off, whereas this finding is logging having never been turned on. Both produce the identical outcome an investigator cares about, an activity blind spot on this bucket, so the technique is mapped here as the closest honest match rather than left unmapped.",
      "S3_LIFECYCLE_MISSING": "T1530 covers this by extension, same reasoning as S3_ENCRYPTION_DISABLED: unbounded retention means more objects are sitting in the bucket for an attacker to collect if they do gain access. A secondary consequence, not a primary enablement, but defensible on the same grounds as the encryption mapping.",
      "S3_TLS_NOT_ENFORCED": "T1557 (Adversary-in-the-Middle) is not part of the Cloud IaaS sub-matrix this file otherwise draws from — it's a general network technique. Included anyway as the closest honest fit: an attacker positioned on the network path can read plaintext S3 requests when TLS is not enforced. Noted as a departure from the sub-matrix scope rather than presented as if it belonged there.",
      "KMS_KEY_ROTATION_DISABLED": "T1530 by extension, same reasoning as the S3 encryption findings: a key that never rotates means a single compromise of the key material exposes everything ever encrypted under it, for as long as that data exists. Secondary consequence, not a primary enablement.",
      "KMS_KEY_PENDING_DELETION": "T1485 (Data Destruction) — the same technique S3_VERSIONING_DISABLED maps to, but unlike that weaker fit, this one is direct: a completed key deletion makes everything encrypted under it permanently unreadable, which is definitionally data destruction. Attackers have used KMS key deletion this way deliberately against S3-encrypted data. The strongest-confidence mapping in this file after S3_PUBLIC_VIA_ACL.",
      "IAM_ROOT_ACCESS_KEYS_ACTIVE": "T1078.004 (Valid Accounts: Cloud Accounts). Worth contrasting with S3_PUBLIC_ACCESS_BLOCK_DISABLED above: T1078.004 was the wrong mapping there and got corrected to T1580, specifically because PAB being off doesn't involve credentials at all. Active root access keys ARE compromised-or-available credentials in exactly the sense T1078.004 describes — this is the clean, correct use of the technique the earlier correction was implicitly reserving it for.",
      "IAM_ACCOUNT_MFA_NOT_ENABLED": "T1078.004 again, immediately after IAM_ROOT_ACCESS_KEYS_ACTIVE. This is deliberate reuse, not an oversight: a password with no second factor is exactly as much a 'valid account' credential-compromise vector as an active access key is, for the same root identity. Flagged here so back-to-back identical references read as reasoned rather than copy-pasted.",
      "IAM_PASSWORD_POLICY_WEAK": "T1110 (Brute Force), a new technique for this file rather than a third T1078.004 reuse. The distinction matters: T1078.004 describes a credential being usable once an attacker already has it, whereas T1110 describes the mechanism that gets them a working credential in the first place — a weak or absent length policy is what makes passwords guessable or crackable at all."
    }
  },

  "S3_PUBLIC_VIA_ACL": [
    {"framework": "mitre_attack", "reference_id": "T1530", "label": "Data from Cloud Storage (Collection tactic)"}
  ],

  "S3_PUBLIC_ACCESS_BLOCK_DISABLED": [
    {"framework": "mitre_attack", "reference_id": "T1580", "label": "Cloud Infrastructure Discovery (Discovery tactic)"}
  ],

  "S3_ENCRYPTION_DISABLED": [
    {"framework": "mitre_attack", "reference_id": "T1530", "label": "Data from Cloud Storage (Collection tactic)"}
  ],

  "S3_VERSIONING_DISABLED": [
    {"framework": "mitre_attack", "reference_id": "T1485", "label": "Data Destruction (Impact tactic)"}
  ],

  "S3_LOGGING_DISABLED": [
    {"framework": "mitre_attack", "reference_id": "T1562.008", "label": "Disable or Modify Cloud Logs (Defense Evasion tactic)"}
  ],

  "S3_LIFECYCLE_MISSING": [
    {"framework": "mitre_attack", "reference_id": "T1530", "label": "Data from Cloud Storage (Collection tactic)"}
  ],

  "S3_TLS_NOT_ENFORCED": [
    {"framework": "mitre_attack", "reference_id": "T1557", "label": "Adversary-in-the-Middle (Collection tactic)"}
  ],

  "KMS_KEY_ROTATION_DISABLED": [
    {"framework": "mitre_attack", "reference_id": "T1530", "label": "Data from Cloud Storage (Collection tactic)"}
  ],

  "KMS_KEY_PENDING_DELETION": [
    {"framework": "mitre_attack", "reference_id": "T1485", "label": "Data Destruction (Impact tactic)"}
  ],

  "IAM_ROOT_ACCESS_KEYS_ACTIVE": [
    {"framework": "mitre_attack", "reference_id": "T1078.004", "label": "Valid Accounts: Cloud Accounts (Initial Access tactic)"}
  ],

  "IAM_ACCOUNT_MFA_NOT_ENABLED": [
    {"framework": "mitre_attack", "reference_id": "T1078.004", "label": "Valid Accounts: Cloud Accounts (Initial Access tactic)"}
  ],

  "IAM_PASSWORD_POLICY_WEAK": [
    {"framework": "mitre_attack", "reference_id": "T1110", "label": "Brute Force (Credential Access tactic)"}
  ]
}